dotenv
fastmcp<=2.12.3
mcp<=1.11.0
jinja2>=3.1.6
langchain>=1.0.0
langchain_core>=1.0.0
langchain_ollama>=1.0.0
langgraph>=1.0.1
langgraph-cli[inmem]>=0.4.4
langsmith>=0.4.37
numpy>=2.3.4
# Capped below 8: newer pytest breaks ROS's system-wide launch_testing pytest
# plugin (its pytest_pycollect_makemodule hookimpl uses an argument name pytest
# removed in 8.0), which loads for every colcon test invocation on the runner.
pytest>=7.4.4,<8
pytest-cov>=7.0.0
pytest-mock>=3.15.1